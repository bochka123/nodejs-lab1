export function findUniq(arr: number[]): number {
    if(arr.length < 3)
        throw new Error("Array must contain at least 3 numbers.");

    let result : number;
    let sample : number;

    if(arr[0] === arr[1])
        sample = arr[0];
    else


    return result;
}
